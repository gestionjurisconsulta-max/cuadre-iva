# Versiones fijadas a proposito. Esta herramienta produce cifras que se declaran,
# y un cambio de version de pandas puede mover un redondeo sin avisar de nada: el
# primer arreglo del repo fue justamente eso, el paso a pandas 2.0.
#
# Estas son las versiones con las que pasan los dos tests. Para subir alguna:
# cambia el numero, pasa tests/test_basico.py y tests/test_regresion.py, y sube
# el cambio solo si los dos siguen en verde.
pandas==3.0.2
openpyxl==3.1.5
jinja2==3.1.6
streamlit==1.57.0

# Historico en PostgreSQL. psycopg es el driver (la 3, no psycopg2) y SQLAlchemy
# lo necesita ademas pandas para leer y escribir tablas contra algo que no sea
# SQLite. El motor se levanta con: docker compose up -d db
sqlalchemy==2.0.46
psycopg[binary]==3.2.12

# La API. uvicorn es el servidor y python-multipart lo necesita FastAPI para
# aceptar los ficheros subidos en el formulario.
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27
